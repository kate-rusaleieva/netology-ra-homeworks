import PropTypes from 'prop-types';

const propTypes = {
    text: PropTypes.string.isRequired,
    link: PropTypes.string.isRequired
};

/* Компонент возвращает пункт меню */
function MenuItem () {

}


MenuItem.propTypes = propTypes;

export default MenuItem